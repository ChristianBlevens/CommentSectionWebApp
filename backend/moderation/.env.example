# Moderation Service Environment Variables

# Server Configuration
NODE_ENV=production
PORT=3001

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres123
DB_NAME=moderation_db

# Admin Configuration (Required)
ADMIN_KEY=your_secure_admin_key_here

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:8080

# Service Ports (for moderation reference in docker-compose)
MODERATION_PORT=3001

# Optional: NLP Configuration
# SENTIMENT_THRESHOLD=-3
# SPAM_SCORE_THRESHOLD=0.7
# CAPS_RATIO_THRESHOLD=0.8
# MIN_TRUST_SCORE=0.1
# MAX_TRUST_SCORE=1.0

# Optional: Blocked Words Refresh Interval (milliseconds)
# BLOCKED_WORDS_REFRESH_INTERVAL=60000